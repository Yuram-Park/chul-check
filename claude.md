# Chul-check 앱 기획

## 1. 프로젝트 개요
- 프로젝트명: 출첵(Chul-Check)
- 목적: 종이 출석부를 대체하여 실시간으로 예배 출석 현황을 파악하고, 데이터 기반의 청년부 관리를 지원함.
- 대상: 리더(셀 관리), 마을장(마을 관리), 간사/교역자(전체 관리)
- 안드로이드, iOS, 웹서비스 모두 사용 가능하게 앱을 만들 예정

## 2. 개발 스택 및 환경

- Framework: Flutter (Dart)
- Target: iOS, Android, Web (간사용)
- State Management: Provider 또는 Bloc (출석 상태 실시간 반영)
- Backend: Spring Boot (REST API)
- Database: MySQL/PostgreSQL

## 3. 사용자 권한 및 주요 기능 (Permissions)

앱 내 권한은 교회의 계층 구조를 그대로 반영합니다.

권한|접근 범위|핵심 액션
리더|본인 셀원|셀원 출석 체크(대예배/청년부), 출석 수정, 셀원 프로필 열람
마을장|소속 마을 리더 & 셀원|리더 출석 체크, 셀원 대리 출석 체크, 마을 전체 현황 조회
간사/교역자|공동체 전체|전체 통계 조회, 전체 데이터 관리 및 수정 (Admin)

## 4. 권한별 유저 스토리 및 UI 흐름

A. 리더 (Leader) - "심플한 리스트와 탭"
로그인: 아이폰의 전화번호 자동 완성 기능을 활용한 간편 로그인.

출첵 메인: * NavigationView 상단에 DatePicker를 배치 (Compact 스타일).

셀원 리스트: List 또는 LazyVStack을 활용하여 셀원 카드를 나열.

출석 입력: 각 셀원 이름 우측에 Menu 버튼이나 Segmented Control을 두어 [출석/결석/지각/기타]를 한 번의 터치로 변경.

셀원 상세: 이름을 누르면 SwiftUI의 .sheet를 통해 하단에서 바텀 시트가 올라오며 성별, 전화번호(Tapping 시 바로 전화 연결) 등 확인.

제출: 하단에 고정된(Sticky) [제출하기] 버튼. 완료 시 Alert 또는 Toast로 "제출되었습니다" 알림.

B. 마을장 (Village Master) - "계층형 탐색"
마을 출석부: * 마을 내 리더들의 출석 상태를 한눈에 볼 수 있는 그리드(Grid) 또는 표 형태의 대시보드.

리더의 이름 옆에 [셀 출석부] 버튼 배치.

대리 출첵: * [셀 출석부] 클릭 시 리더의 출석 페이지와 동일한 UI로 NavigationLink를 통해 이동.

마을장이 입력한 데이터도 리더의 데이터와 동일하게 동기화됨.

C. 공통 기능 - "달력 기반 조회"
캘린더 뷰: MultiDatePicker 또는 커스텀 달력을 통해 과거 날짜 선택.

기록 확인 및 수정: * 데이터가 존재하는 날짜를 선택하면 화면이 **'조회 모드'**로 전환.

하단 [수정하기] 버튼을 누르면 입력 필드가 활성화되는 **'편집 모드'**로 변경.


## 5. Admin 페이지

### 1) 권한 관리 (Role Management)
사용자 역할은 크게 Super Admin과 **Admin(간사)**으로 구분합니다.

구분|권한 범위|주요 특징
Super Admin|시스템 전체 관리|"'리더 스토리' 페이지 접근 가능, 권한 부여 가능"
Admin (간사)|셀원 및 출석 관리|"셀원 리스트 조회, 등록, 수정, 엑셀 내보내기"

### 2) 페이지 구성 및 기능 상세

A. 전체 셀원 확인 페이지
모든 셀원의 정보를 한눈에 파악하고 필터링할 수 있는 메인 관리 화면입니다.

UI 구조: 상단 필터바 + 중앙 데이터 테이블 + 하단 페이지네이션

테이블 명세:

노출 개수: 페이지당 20개 Row

컬럼 구성: 번호, 마을장, 리더, 이름, 성별, 또래, 생년월일, 전화번호, 라인아웃 여부(Y/N), 등록일

주요 기능:

컬럼별 필터: 각 컬럼 헤더에 드롭다운 또는 텍스트 입력형 필터 적용.

정렬(Sorting): 기본값은 '등록일 내림차순'.

Excel Export: 현재 필터링된 리스트 혹은 전체 리스트를 .xlsx 파일로 추출.

B. 셀원 등록 페이지
새로운 셀원을 대량으로 추가하기 위한 페이지입니다.

진입 경로: 전체 셀원 확인 페이지 우측 상단 [등록하기] 버튼 클릭.

입력 방식: 엑셀 시트 형태의 그리드(Grid) UI.

최대 20명까지 동시 입력 가능.

(추후 검토) 엑셀 데이터 복사/붙여넣기(Copy & Paste) 기능.

하단 버튼:

[초기화]: 입력한 모든 내용을 지우고 빈 그리드로 복구.

[등록하기]: DB 저장 후 '전체 셀원 확인 페이지'로 리다이렉트.

등록 후 로직: 리스트로 돌아왔을 때 모든 정렬/필터가 초기화되며, **최신 등록순(내림차순)**으로 정렬되어 방금 넣은 데이터가 최상단에 노출됩니다.

C. 스토리 및 권한관리 페이지

해당 메뉴 클릭 시 하위 선택 목록(Landing Page) 노출:
- 리더 스토리: 셀원들의 상세 이력 및 특이사항 관리 페이지로 이동.
- 권한 관리: 간사 승인 및 Super Admin 권한 부여 페이지로 이동.

'리더 스토리' 페이지 상세 기능
- 기존 '전체 셀원 확인' 페이지의 기능을 계승하되, 데이터의 깊이와 확장성을 확보
- UI 및 테이블 구성
    - 데이터 그리드:
        - 가로 스크롤(Horizontal Scroll) 도입: 컬럼 수가 많으므로 고정 폭을 가진 테이블에 가로 스크롤을 적용하여 가독성을 확보합니다. (번호, 이름 등 핵심 컬럼은 좌측 고정(Sticky) 권장)
        - 추가 컬럼 목록:
            - 기본 정보(이름, 또래 등) + 라인아웃 요청 사유, 현재 상태, 이전 공동체, 훈련 및 섬김 여부, 코멘트 1, 코멘트 2
            - 비고: 향후 컬럼이 추가될 수 있도록 유연한 Table Schema 구조 채택.
    - 주요 기능:
        - Excel Export: 리더 스토리 전체 데이터를 엑셀 파일로 추출.
        - Excel 필터: 모든 추가 컬럼에 대해 텍스트 검색 및 드롭다운 필터 지원.
- 데이터 등록 프로세스 (Dual-Mode)
    - 우측 상단 [등록하기] 버튼 클릭 시 하위 선택 버튼 2개가 노출됩니다.
    등록 방식|상세 설명
    직접 등록하기|기존 셀원 등록과 동일하게 엑셀 형태의 그리드(20줄) 제공. 직접 타이핑하여 입력.
    엑셀 업로드|정해진 양식(Template)의 엑셀 파일을 업로드하면 시스템이 데이터를 파싱하여 일괄 DB 저장.

### 3) 데이터 및 통계 기능 (추가 기능)

A. 개인별 출석률 계산
- 계산식: (해당인원의 출석 횟수 / 전체 예배 일수) * 100 
- 활용: 셀원 상세 보기나 리스트의 별도 컬럼으로 배치하여 관리자가 한눈에 성실도를 파악할 수 있게 함.

B. 엑셀 내보내기 (Export)
- 형식: .xlsx
- 내용: 필터링된 셀원 명단뿐만 아니라, 출석률 데이터가 포함된 리포트 형식으로 추출 가능하게 구성.